## Common Commands
- `npm install` – install dependencies.
- `npm run build` – compile TypeScript into `lib/`.
- `npm run dev` – watch mode compilation.
- `firebase emulators:start --only functions,firestore` – run local emulators.
